node_modules/
dist/
*.log
.DS_Store
# Installed pack copies carry project-local omm.jsonc, which may hold secrets.
.claude/
.scratch/
*.tgz
omm-notify.log
# Maintainer-local recon evidence (session IDs, local paths) is never published.
docs/muse-recon/
